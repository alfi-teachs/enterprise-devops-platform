apiVersion: v1
kind: ConfigMap

metadata:
  name: enterprise-config
  namespace: enterprise-devops

data:
  APP_NAME: "Enterprise DevOps Platform"
  APP_ENV: "development"
  LOG_LEVEL: "INFO"
  APP_PORT: "80"