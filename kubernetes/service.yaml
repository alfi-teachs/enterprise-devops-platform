apiVersion: v1
kind: Service

metadata:
  name: enterprise-devops-app
  namespace: enterprise-devops

spec:
  type: ClusterIP

  selector:
    app: enterprise-devops-app

  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
      
      