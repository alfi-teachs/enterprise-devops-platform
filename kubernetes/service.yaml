apiVersion: v1
kind: Service

metadata:
  name: enterprise-devops-app
  namespace: enterprise-devops

spec:
  type: NodePort

  selector:
    app: enterprise-devops-app

  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
      nodePort: 30472
      